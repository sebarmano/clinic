<p id="notice"><%= notice %></p>

<div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8 sm:flex sm:items-center sm:justify-between">
  <h1 class="text-2xl font-semibold text-gray-900">Clientes</h1>

  <div class="mt-3 sm:mt-0 sm:ml-4">
    <%= link_to "Crear nuevo cliente", new_customer_path, class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white visited:text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
  </div>
</div>

<div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8 py-4">
  <!-- This example requires Tailwind CSS v2.0+ -->
  <div class="bg-white shadow overflow-hidden sm:rounded-md">
    <ul role="list" class="divide-y divide-gray-200">
      <%= render @customers %>
    </ul>
  </div>

  <br>
</div>
